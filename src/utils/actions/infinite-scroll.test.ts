// Implement when `svelte-htm` package supports svelte 4
describe('infiniteScroll', () => {
	it.todo('triggers callback when 10% above bottom of element just appeared in viewport');

	it.todo('does not trigger callback when 11% above bottom of element just appeared in viewport');

	it.todo('does not trigger callback when bottom of element just appeared in viewport');

	it.todo('does not trigger callback when top of element just appeared in viewport');

	it.todo('allows disconnecting from infinite scrolling');
});
